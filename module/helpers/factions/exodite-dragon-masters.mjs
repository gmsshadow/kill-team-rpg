/**
 * Exodite Dragon Masters.
 *
 * Generated by tools/import_catalogue.py from the New Recruit catalogue,
 * which tracks Kill Team 2018 including errata and the Elites and Commanders
 * expansions. Do not hand-edit: re-run the importer instead.
 *
 * 0 models, 0 weapons.
 *
 * Ability text is imported as prose. Machine-readable rules are tagged
 * separately, by hand, in module/rules/ - deciding what an ability does is a
 * judgement call and is not safe to automate.
 */

export const EXODITE_DRAGON_MASTERS_WEAPONS = [];

export const EXODITE_DRAGON_MASTERS_MODELS = [];

const FACTION = "Exodite Dragon Masters";

export const EXODITE_DRAGON_MASTERS_ITEMS = EXODITE_DRAGON_MASTERS_WEAPONS.map(w => ({ ...w, itemType: "weapon", faction: FACTION }));

export const EXODITE_DRAGON_MASTERS_MODEL_ITEMS = EXODITE_DRAGON_MASTERS_MODELS.map(m => ({ ...m, itemType: "model", faction: FACTION, rules: [] }));
